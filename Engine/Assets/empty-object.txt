package Assets.Objects;

import Engine.Entity.Object;
import Engine.Utils.StdBehaviour;

import Engine.Components.*;

import Engine.*;
import Assets.*;

public class [CLASS NAME] extends Object implements StdBehaviour{

    public [CLASS NAME]() { super("[CLASS NAME]"); Object.behaviours.add(getBehaviour()); Object.objects.add(this); Start();}
    private StdBehaviour getBehaviour(){ return this; }
    boolean updatable = false;

    //Called once the object is initialized
    @Override
    public void Start() {
        
        updatable = true;
    }

    //Called every frame
    @Override
    public void Update() {
        
        if(updatable) {

        }
    }

    @Override
    public void DrawGUI(Graphics2D g) {
        
    }
    
}
